# Запуск проекта

```yaml
python3 -m venv venv
source venv/bin/activate
pip3 install -r requirements.txt
uvicorn main:app --reload
```

## Документация

Доступна после запуска по адресу: http://localhost:8000/docs
